import { Typography } from "@material-ui/core";
import React from "react";

const Profile = () => {
  return (
    <div>
      <Typography variant="h2">porfile page</Typography>
    </div>
  );
};

export default Profile;
